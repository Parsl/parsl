language: python
python:
  - "3.5"
  - "3.6"

# command to install dependencies
install:
    - pip install -r requirements.txt
    - python setup.py install

# command to run tests
script:
    - pip install -r test-requirements.txt
    - ipcluster start -n 2 &
    - sleep 30
    - nosetests -v parsl/tests/test_threads
    - ipcluster stop
