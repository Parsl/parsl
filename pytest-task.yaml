apiVersion: batch/v1
kind: Job
metadata:
  name: pytest
spec:
  activeDeadlineSeconds: 600
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: pytest
        image: parslimg:a
        command: ["bash", "runme.sh"]

