rm -rf *~ *txt *out *err outputs data runinfo

for dir in */
    do
        echo "Cleaning dir: $dir"
        cd $dir
        rm -rf *txt *out *err outputs *~ runinfo ipengine.json
        cd ..
    done

find . -path '*/runinfo' -type d | xargs rm -rf
