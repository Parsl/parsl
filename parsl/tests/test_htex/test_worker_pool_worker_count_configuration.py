import pytest

# There are various parameters which control how the Manager computes the
# number of workers it should start.
# This is distinct from the calculation in executor.py which computes
# something similar but different.
# and perhaps those should be unified? (!)
